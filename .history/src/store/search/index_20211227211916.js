// search组件的小仓库
import { reqGetSearchInfo } from "@/api";

const state = {};
const mutations = {};
const actions = {
    asynv
};
const getters = {};
export default{
    state,
    mutations,
    actions,
    getters
}