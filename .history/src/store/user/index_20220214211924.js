// 登录与注册的模块
import {reqGetCode} from 
const state = {};
const mutations = {};
const actions = {
    async getCode(){
        let result = reqGetCode(phone);
        if()
    }
};
const getters = {};
export default{
    state,
    mutations,
    actions,
    getters
}