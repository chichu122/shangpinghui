const state = {};
const mutations = {};
const action = {
    await getCartList({commit}){
        let re
    }
};
const getters = {};
export default{
    state,
    mutations,
    action,
    getters
}