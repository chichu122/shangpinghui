const state = {};
const mutations = {};
const action = {
    await getCartList({commit}){
        let resu
    }
};
const getters = {};
export default{
    state,
    mutations,
    action,
    getters
}