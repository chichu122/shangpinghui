const state = {};
const mutations = {};
const action = {
    await getCartList({commit}){
        let result = as
    }
};
const getters = {};
export default{
    state,
    mutations,
    action,
    getters
}