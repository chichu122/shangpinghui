const state = {};
const mutations = {};
const action = {
    await getCartList({commit}){
        let result = 
    }
};
const getters = {};
export default{
    state,
    mutations,
    action,
    getters
}