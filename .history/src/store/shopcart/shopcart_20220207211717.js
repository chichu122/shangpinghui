const state = {};
const mutations = {};
const action = {
    await getCartList({commit}){
        let res
    }
};
const getters = {};
export default{
    state,
    mutations,
    action,
    getters
}