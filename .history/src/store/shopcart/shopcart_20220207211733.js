const state = {};
const mutations = {};
const action = {
    await getCartList({commit}){
        let result = async req
    }
};
const getters = {};
export default{
    state,
    mutations,
    action,
    getters
}