const state = {};
const mutations = {};
const actions = {
    async getUserAddress({commit}){
        let result = await
    }
};
const getters = {};

export default {
    state,
    mutations,
    actions,
    getters,
}