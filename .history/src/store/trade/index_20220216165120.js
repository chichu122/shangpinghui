const state = {};
const mutations = {};
const actions = {
    async getUserAddress({commit}){
        let result = awai
    }
};
const getters = {};

export default {
    state,
    mutations,
    actions,
    getters,
}